const calcTip = function (bil) {
  return bil >= 50 && bil <= 300 ? bil * 0.15 : bil * 0.2;
};


const bills = [125, 555, 44];
const tips = [];
const totals = [];

function calcTipAndTotal(bills) {

}